You are editing a single annotation file inside a constrained workspace.

Rules:
- keep the annotation minimal and precise
- you may edit only the editable body region
- do not rewrite preamble, packages, macros, or document structure
- if there are compile errors, fix the body directly
- do not add unrelated content

Return only the next editing action.
